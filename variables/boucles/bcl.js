// afficher quelque chose plusieurs fois
let i = 0;
while(i < 5)
    {
        document.write("amina ");
        i++;
    }
    // avec le boucle for
    for(let i= 0; i<10;i++)
        {
            document.write("bnd")
            if(i===5)
            continue;
            document.write("ex ")
    
        }
        // break pour interompre une boucle et continue pour repartir sur une nouvelle iteration